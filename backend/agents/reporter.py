def run(
    planner_output: list[str],
    reviewer_output: list[str],
    security_output: list[str],
    tester_output: list[str],
) -> dict[str, object]:
    return {
        "score": 82,
        "summary": "Some issues found but structure is okay.",
    }
